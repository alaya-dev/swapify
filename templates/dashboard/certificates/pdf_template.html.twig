<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Certificat de Participation</title>
    <style>
        /* General Styles */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .certificate {
            width: 900px;
            height: 550px;
            border: 20px solid #28a745;
            background-color: #ffffff;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            position: relative;
            text-align: center;
            padding: 40px;
        }

        .certificate::before {
            content: '';
            position: absolute;
            top: -20px;
            left: -20px;
            right: -20px;
            bottom: -20px;
            border: 10px solid rgba(153, 27, 27, 0.1);
            z-index: -1;
        }

        .header {
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 48px;
            color: #28a745;
            margin: 0;
            text-transform: uppercase;
        }

        .header h2 {
            font-size: 24px;
            color: #333;
            margin: 10px 0 0;
        }

        .content {
            margin: 40px 0;
        }

        .content h3 {
            font-size: 28px;
            color: #28a745;
            margin: 0 0 20px;
        }

        .content p {
            font-size: 20px;
            color: #555;
            margin: 10px 0;
        }

        .footer {
            margin-top: 40px;
        }

        .footer p {
            font-size: 16px;
            color: #777;
            margin: 5px 0;
        }
.signature {
            font-family: 'Brush Script MT', cursive;
            font-size: 24px;
            color: #333;
            display: inline-block;
            border-bottom: 2px solid #000;
            padding: 5px 20px;
            margin-top: 5px;
        }
        .badge {
    position: absolute;
    bottom: 20px;
    right: 30px;
 
    

    display: flex;
    justify-content: center;
    align-items: center;
    /* overflow: hidden; Ensure the image doesn't overflow */
}
.badge img {
    position: absolute;
    bottom: 0px;
    right: 0px;
    width: 200px;
    height: 200px;
    
}
        /* Watermark */
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 120px;
            color: rgba(27, 153, 136, 0.1);
            font-weight: bold;
            text-transform: uppercase;
            pointer-events: none;
            user-select: none;
        }
    </style>
</head>
<body>
    <div class="certificate">
        <!-- Watermark -->
        <div class="watermark">SWAPIFY</div>

        <!-- Header -->
        <div class="header">
            <h1>Certificat</h1>
            <h2>de Participation</h2>
        </div>

        <!-- Content -->
        <div class="content">
            <h3>Ce certificat est décerné à</h3>
            <p>{{ app.user.nom }} {{ app.user.prenom }}</p>
            <h3>Pour avoir participé à l'événement</h3>
            <p>{{ event.title }}</p>
            <p>Du {{ event.dateDebut|date('d/m/Y') }} au {{ event.dateFin|date('d/m/Y') }}</p>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Organisé par : {{ event.orgniser.nom }} {{ event.orgniser.prenom }}</p>
            <p>Date d'émission : {{ "now"|date('d/m/Y') }}</p>
        </div>

        <div class="badge signature">
                    <h6>Signature du Propriétaire de l'événement :</h6>

             <div> {{ event.orgniser.nom }} {{ event.orgniser.prenom }}</div>
      </div>

    </div>
</body>
</html>
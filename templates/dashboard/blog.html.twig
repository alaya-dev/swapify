{% extends 'dashBord_user.html.twig' %}

{% block content %}
    <div class="w-full overflow-hidden border-t flex flex-col">
        <div class="flex flex-row"> <!-- Container for sidebar and main content -->
            <!-- Second Sidebar -->
            <div class="w-1/5 bg-gray-100 p-6"> <!-- Sidebar width and styling -->
                <ul class="space-y-4"> <!-- List of choices -->
                    <li>
                        <a href="{{ path('app_blog_index') }}" class="text-gray-700 hover:text-gray-900">voir blogs</a>
                    </li>
                    <li>
                        <a href="{{ path('app_blog_new') }}" class="text-gray-700 hover:text-gray-900">creer blogs </a>
                    </li>
                    <li>
                        <a href="#" class="text-gray-700 hover:text-gray-900">Choice 3</a>
                    </li>
                    <li>
                        <a href="#" class="text-gray-700 hover:text-gray-900">Choice 4</a>
                    </li>
                </ul>
            </div>

            <!-- Main Content Area -->
            <main class="w-4/5 flex-grow p-6"> <!-- Main content width and styling -->
                <p class="text-xl pb-6 flex items-center">
                    <i class="fas fa-newspaper mr-3"></i> Annonce  
                </p>
                <!-- Add your main content here if needed -->
            </main>
        </div>
    </div>
{% endblock %}
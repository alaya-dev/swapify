{% block body %}
    <section class="w-full mx-auto max-w-screen-xl p-4">
        <div class="grid grid-cols-1 md:grid-cols-6 gap-6">
            
            <!-- Profile Card -->
            {{ include('dashboard/profile_user/components/profile_card.html.twig') }}

            <!-- Edit Details Form -->
            {{ include('dashboard/profile_user/components/edit_details_form.html.twig') }}

            <!-- Offers Section -->
            {{ include('dashboard/profile_user/components/offers_section.html.twig') }}

            <!-- Products Section -->
            {{ include('dashboard/profile_user/components/products_section.html.twig') }}
            
        </div>
    </section>
{% endblock %}
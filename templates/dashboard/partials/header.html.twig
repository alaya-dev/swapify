<script src="https://cdn.jsdelivr.net/npm/alpinejs@2.8.2/dist/alpine.min.js" defer></script>
<header class="w-full items-center bg-white py-2 px-6 hidden sm:flex">
    <div class="w-1/2"></div>
    <div x-data="{ isOpen: false }" class="relative w-1/2 flex justify-end">
        <button @click="isOpen = !isOpen" class="relative z-10 w-12 h-12 rounded-full border-4 border-gray-400 hover:border-gray-300 focus:border-gray-300 focus:outline-none">
            <img src="#" alt="avatar">
        </button>
        <button x-show="isOpen" @click="isOpen = false" class="h-full w-full fixed inset-0 cursor-default"></button>
        <div x-show="isOpen" class="absolute w-32 bg-white rounded-lg shadow-lg py-2 mt-16">
            <a href="/" class="block px-4 py-2 account-link hover:text-white">Home</a>
            <a href="#" class="block px-4 py-2 account-link hover:text-white">Support</a>
            <a href="/logout" class="block px-4 py-2 account-link hover:text-white">Sign Out</a>
        </div>
    </div>
</header>

<!-- Mobile Header & Nav -->
<header x-data="{ isOpen: false }" class="w-full bg-sidebar py-5 px-6 sm:hidden">
    <div class="flex items-center justify-between">
        <a href="index.html" class="text-white text-3xl font-semibold uppercase hover:text-gray-300">User</a>
        <button @click="isOpen = !isOpen" class="text-white text-3xl focus:outline-none">
            <i x-show="!isOpen" class="fas fa-bars"></i>
            <i x-show="isOpen" class="fas fa-times"></i>
        </button>
    </div>

    <!-- Dropdown Nav -->
    <nav :class="isOpen ? 'flex' : 'hidden'" class="flex flex-col pt-4">
        <a href="{{ path('app_dashboard_client') }}" class="flex items-center {{ app.request.attributes.get('_route') == 'app_dashboard_client' ? 'active-nav-link' : 'text-white opacity-75 hover:opacity-100' }} py-2 pl-4 nav-item">
            <i class="fas fa-tachometer-alt mr-3"></i>
            Profile
        </a>
        <a href="{{ path('souk') }}" class="flex items-center {{ app.request.attributes.get('_route') == 'souk' ? 'active-nav-link' : 'text-white opacity-75 hover:opacity-100' }} py-2 pl-4 nav-item">
            <i class="fas fa-sticky-note mr-3"></i>
            Souk
        </a>
        <a href="{{ path('annonce') }}" class="flex items-center {{ app.request.attributes.get('_route') == 'annonce' ? 'active-nav-link' : 'text-white opacity-75 hover:opacity-100' }} py-2 pl-4 nav-item">
            <i class="fas fa-table mr-3"></i>
            Annonce
        </a>
        <a href="{{ path('blog') }}" class="flex items-center {{ app.request.attributes.get('_route') == 'blog' ? 'active-nav-link' : 'text-white opacity-75 hover:opacity-100' }} py-2 pl-4 nav-item">
            <i class="fas fa-align-left mr-3"></i>
            Blog
        </a>
        <a href="{{ path('evenment') }}" class="flex items-center {{ app.request.attributes.get('_route') == 'evenment' ? 'active-nav-link' : 'text-white opacity-75 hover:opacity-100' }} py-2 pl-4 nav-item">
            <i class="fas fa-tablet-alt mr-3"></i>
            Evenment
        </a>
        <a href="{{ path('livraison') }}" class="flex items-center {{ app.request.attributes.get('_route') == 'livraison' ? 'active-nav-link' : 'text-white opacity-75 hover:opacity-100' }} py-2 pl-4 nav-item">
            <i class="fas fa-calendar mr-3"></i>
            Livraison
        </a>
    </nav>
</header>

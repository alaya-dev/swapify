{% extends 'base.html.twig' %}
{% block body %}
    <div class="grid grid-cols-1 md:grid-cols-2 h-screen p-4 gap-4 items-center mt-16 overflow-hidden ">
        <div class="p-10 w-full max-w-lg mx-auto h-[100vh] ">
            <h3 class="text-3xl font-semibold text-gray-800 mb-8">
                Connectez-vous sur <span class="font-handwriting text-green-600">SWAPIFY</span>
            </h3>
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="p-4 mb-4 text-sm text-red-700 bg-red-100 rounded-lg">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            <div class="mb-6">
                <label for="email" class="block text-gray-700 font-bold mb-2">E-mail *</label>
                <input
                    type="email"
                    id="email"
                    placeholder="alouioussema697@salman.tn"
                    class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-200 focus:outline-none"
                />
            </div>

            <!-- Password Input -->
            <div class="mb-6">
                <label for="mdp" class="block text-gray-700 font-bold mb-2">Mot de passe *</label>
                <input
                    type="password"
                    id="mdp"
                    placeholder="***********"
                    class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-200 focus:outline-none"
                />
            </div>

            <!-- Login Button -->
            <button class="w-full bg-green-600 text-white py-3 rounded-lg font-semibold text-lg hover:bg-green-700 transition duration-200">
                Se connecter
            </button>

            <!-- Forgot Password Link -->
            <div class="mt-4 text-center">
                <a href="#" class="text-gray-600 hover:underline">Mot de passe oublié ?</a>
            </div>

            <!-- Separator Line -->
            <div class="my-6 w-full h-px bg-gradient-to-r from-transparent via-gray-400 to-transparent"></div>

            <!-- Create Account Button -->
            <a href="{{path('app_register')}}" class="w-full block text-center bg-gray-50 border border-gray-200 text-gray-800 py-3 rounded-lg font-semibold hover:bg-gray-100 transition duration-200">
                Créez votre compte dès maintenant
            </a>
        </div>

        <!-- Right Section -->
        <div class="hidden md:grid grid-cols-3 gap-4 overflow-hidden h-[100vh]">
            <!-- Column 1 -->
            <div class="grid grid-rows-6 gap-4 animate-slide">
                <div class="h-56 bg-pink-100 rounded-lg flex items-center justify-center text-4xl">🚗</div>
                <div class="h-56 bg-cyan-100 rounded-lg flex items-center justify-center text-4xl">📱</div>
                <div class="h-56 bg-green-100 rounded-lg flex items-center justify-center text-4xl">📚</div>
                <!-- Duplicate items -->
                <div class="h-56 bg-pink-100 rounded-lg flex items-center justify-center text-4xl">🚗</div>
                <div class="h-56 bg-cyan-100 rounded-lg flex items-center justify-center text-4xl">📱</div>
                <div class="h-56 bg-green-100 rounded-lg flex items-center justify-center text-4xl">📚</div>
            </div>

            <!-- Column 2 -->
            <div class="grid grid-rows-6 gap-4 animate-slide-reverse">
                <div class="h-56 bg-green-100 rounded-lg flex items-center justify-center text-4xl">👕</div>
                <div class="h-56 bg-pink-100 rounded-lg flex items-center justify-center text-4xl">⛺</div>
                <div class="h-56 bg-cyan-100 rounded-lg flex items-center justify-center text-4xl">🪑</div>
                <!-- Duplicate items -->
                <div class="h-56 bg-green-100 rounded-lg flex items-center justify-center text-4xl">👕</div>
                <div class="h-56 bg-pink-100 rounded-lg flex items-center justify-center text-4xl">⛺</div>
                <div class="h-56 bg-cyan-100 rounded-lg flex items-center justify-center text-4xl">🪑</div>
            </div>

            <!-- Column 3 -->
            <div class="grid grid-rows-6 gap-4 animate-slide">
                <div class="h-56 bg-cyan-100 rounded-lg flex items-center justify-center text-4xl">🚗</div>
                <div class="h-56 bg-green-100 rounded-lg flex items-center justify-center text-4xl">📱</div>
                <div class="h-56 bg-pink-100 rounded-lg flex items-center justify-center text-4xl">🪑</div>
                <!-- Duplicate items -->
                <div class="h-56 bg-cyan-100 rounded-lg flex items-center justify-center text-4xl">🚗</div>
                <div class="h-56 bg-green-100 rounded-lg flex items-center justify-center text-4xl">📱</div>
                <div class="h-56 bg-pink-100 rounded-lg flex items-center justify-center text-4xl">🪑</div>
            </div>
        </div>
    </div>

    <!-- Animation Styles -->
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Edu+AU+VIC+WA+NT+Hand:wght@400..700&family=Gruppo&display=swap');

<<<<<<< HEAD
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            position: relative;
        }

        .navbar {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            background-color: white;
            box-shadow: rgba(60, 64, 67, 0.3) 0px 1px 2px 0px, rgba(60, 64, 67, 0.15) 0px 2px 6px 2px;
            display: grid;
            grid-template-columns: 35% 60% 5%;
            align-self: center;
            padding: 10px 20px;
            z-index: 10;
        }

        .home {
            text-decoration: none;
            color: #88c858;
            align-self: center;
            padding-left: 15px;
        }

        .logo {
            font-family: "Edu AU VIC WA NT Hand", serif; 
            color: #88c858;
            font-size: 30px;
            font-weight: 500;
            letter-spacing: .6em;
        }

        .container {
            z-index: 0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            height: 100vh;
            padding: 15px;
            box-sizing: border-box;
        }

        .left-section {
            display: grid;
            align-content: center;
            background-color: white;
            padding: 40px;
            width: 80%;
            overflow: hidden;
        }

        .left-section h3 {
            color: #333;
            margin-bottom: 30px;
            font-size: 30px;
        }

        .left-section h3 span {
            font-family: "Edu AU VIC WA NT Hand", serif;
        }

        .input-group {
            display: grid;
            gap: 5px;
            margin-bottom: 15px;
        }

        label {
            color: #666;
            font-weight: bold;
        }

        .input-field {
            padding: 15px;
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
            box-sizing: border-box;
        }

        .input-field.is-invalid {
            border-color: red;
            background-color: #f8d7da;
        }

        .btn-primary {
            background-color: #88c858;
            border: none;
            padding: 15px;
            border-radius: 10px;
            font-size: 20px;
            font-weight:600;
            cursor: pointer;
            width: 100%;
            margin-top: 15px;
            transition-duration: 0.1s;
        }

        .btn-primary a {
            color: #fff;
            text-decoration: none;
        }

        .btn-primary:hover {
            background-color: #6b9e43;
            transition-duration: 0.1s;
        }

        .separator-container {
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            align-items: center;
            gap: 10px;
            margin: 20px 0;
            color: #666;
        }

        .separator-line {
            border-top: 1px solid #ccc;
        }

        .create-account {
            background-color: #FAFBFC;
            border: 1px solid rgba(27, 31, 35, 0.15);
            border-radius: 6px;
            box-shadow: rgba(27, 31, 35, 0.04) 0 1px 0, rgba(255, 255, 255, 0.25) 0 1px 0 inset;
            transition-duration: 0.1s;
            padding: 15px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            line-height: 20px;
            text-align: center;
            margin-top: 20px;
        }

        .create-account:hover {
            background-color: #F3F4F6;
            text-decoration: none;
            transition-duration: 0.1s;
        }

        .create-account a {
            color: #24292E;
            text-decoration: none;
            font-weight: bold;
        }

        .forgot-password {
            margin-top: 15px;
            text-decoration: none;
        }

        .forgot-password a {
            text-decoration: none;
            color: #666;
        }

        .custom-line {
            width: 80%;
            height: 2px;
            background: linear-gradient(to right, transparent, #888, transparent);
            margin: 20px auto;
        }

        .right-section {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            overflow: hidden;
        }

        .column {
            display: grid;
            grid-template-rows: repeat(6, 1fr);
            gap: 20px;
            animation: slide 8s linear infinite;
        }

        #col-2 {
            animation-direction: reverse;
        }

        .item {
            height: 220px;
            border-radius: 20px;
            display: grid;
            place-items: center;
            font-size: 48px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .item:hover {
            transform: scale(1.05);
        }

=======
>>>>>>> 3dc3f163037b9fd697baa4e9e8b2b2a8247db39c
        @keyframes slide {
            from { transform: translateY(0%); }
            to { transform: translateY(-50%); }
        }

        .animate-slide {
            animation: slide 8s linear infinite;
        }

        .animate-slide-reverse {
            animation: slide 8s linear infinite reverse;
        }


    </style>
<<<<<<< HEAD
</head>
<body>
    <div class="navbar">
        <div class="home"><a href="/register">⬅</a></div>
        <div class="logo">Swapify</div>
        <div class="logo-img">🚗</div>
    </div>
    
    <br><br><br>
    
    <div class="container">
       <div class="left-section">
            <h3>Connectez-vous sur <span>SWAPIFY</span></h3>
                      {% for label, messages in app.flashes %}
                            {% for message in messages %}
                                <div class="alert alert-{{ label }}">
                                    {{ message }}
                                </div>
                            {% endfor %}
                      {% endfor %}

                    <form method="post" novalidate>
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                        <div class="input-group">
                            <label for="email">E-mail</label>
                            <input type="email" id="email" name="email" value="{{ last_username }}" class="input-field" required>
                        </div>

                        <div class="input-group">
                            <label for="password">Mot de passe</label>
                            <input type="password" id="password" name="password" class="input-field" required>
                        </div>

                        <button type="submit" class="btn-primary">Se connecter</button>
                    </form>

            <div class="forgot-password">
                <a href="{{ path('app_forgot_password_request') }}">Mot de passe oublié ?</a>
            </div>

            <div class="custom-line"></div>
            
            <button class="create-account">
                <a href="{{ path('app_register') }}">Créez votre compte dès maintenant</a>
            </button>
        </div>
        <div class="right-section">
            <div class="column" id="col-1">
                <div class="item" style="background-color: #FFE4E1;">🚗</div>
                <div class="item" style="background-color: #E0FFFF;">📱</div>
                <div class="item" style="background-color: #F0FFF0;">📚</div>
                <div class="item" style="background-color: #FFE4E1;">🚗</div>
                <div class="item" style="background-color: #E0FFFF;">📱</div>
                <div class="item" style="background-color: #F0FFF0;">📚</div>
            </div>

            <div class="column" id="col-2">
                <div class="item" style="background-color: #F0FFF0;">👕</div>
                <div class="item" style="background-color: #FFE4E1;">⛺</div>
                <div class="item" style="background-color: #E0FFFF;">🪑</div>
                <div class="item" style="background-color: #F0FFF0;">👕</div>
                <div class="item" style="background-color: #FFE4E1;">⛺</div>
                <div class="item" style="background-color: #E0FFFF;">🪑</div>
            </div>

            <div class="column" id="col-3">
                <div class="item" style="background-color: #E0FFFF;">🚗</div>
                <div class="item" style="background-color: #F0FFF0;">📱</div>
                <div class="item" style="background-color: #FFE4E1;">🪑</div>
                <div class="item" style="background-color: #E0FFFF;">🚗</div>
                <div class="item" style="background-color: #F0FFF0;">📱</div>
                <div class="item" style="background-color: #FFE4E1;">🪑</div>
            </div>
        </div>
    </div>
</body>
</html>
=======

{% endblock %}
>>>>>>> 3dc3f163037b9fd697baa4e9e8b2b2a8247db39c
